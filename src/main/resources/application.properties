# --- Common Settings For All Profiles ---

# Set the default profile to 'dev'. This will be used when running locally.
#spring.profiles.active=dev

# These connection pool settings will apply to all environments
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# ===============================================
# GOOGLE CLOUD SQL (MySQL) CONFIGURATION
# ===============================================

# IMPORTANT: Replace these placeholder values with your actual database credentials.
spring.datasource.username=namaste-village-001
spring.datasource.password=/1FmY(XmjK4FmhEQ

# The name of the database (schema) you created within the Cloud SQL instance.
DB_NAME=namastevillage

# The Instance Connection Name for your Cloud SQL instance.
INSTANCE_CONNECTION_NAME=code-with-intellij-gemini:us-central1:namaste-village-001

# JDBC Connection String using the Cloud SQL Socket Factory
# The Socket Factory creates a secure, local proxy to connect to your Cloud SQL instance.
spring.datasource.url=jdbc:mysql://localhost:3306/${DB_NAME}?cloudSqlInstance=${INSTANCE_CONNECTION_NAME}&socketFactory=com.google.cloud.sql.mysql.SocketFactory&useSSL=false

# Specify the MySQL JDBC driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===============================================
# JPA & HIBERNATE CONFIGURATION (Optional)
# ===============================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===============================================
# SPRING MAIL CONFIGURATION (for sending emails)
# ===============================================
# IMPORTANT: Replace with your actual email credentials. Using a Gmail App Password is recommended.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=codewithbhautik01@gmail.com
spring.mail.password=mspy stpl tvrq sbiu

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# ===============================================
# JWT SECRET CONFIGURATION
# ===============================================
# A secure, 256-bit secret key for signing JWT tokens.
jwt.secret=7336763979244226452948404D635166545DFGSD46A576E5A7234753778214125442A47
